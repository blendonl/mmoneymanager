import { IStoreLocationParser } from '../../interfaces/store-location-parser.interface';
export declare class AlbanianStoreLocationParser implements IStoreLocationParser {
    extractStoreLocation(lines: string[]): string;
}
